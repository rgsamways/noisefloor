# @noisefloor/dashboards

**Partially parked, 2026-09-21.** See `/docs/NOISEFLOOR-CONSOLE-HANDOFF.md` and `PROJECT-PLAN.md` D16.

- `src/primitives/` — **not parked.** `Gauge`, `LinearMeter`, `SegmentBar`, `StackedBars`, `LineTrace` are reusable chart/meter building blocks, likely useful for the console UI.
- `src/crm/`, `src/nms/`, `src/radio/` — **parked.** Case-player dashboard families built for the case-study product. Working, tested code, not deleted — may resume in a later phase.
