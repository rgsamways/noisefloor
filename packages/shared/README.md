# @noisefloor/shared

**Partially parked, 2026-09-21.** See `/docs/NOISEFLOOR-CONSOLE-HANDOFF.md` and `PROJECT-PLAN.md` D16.

- `src/schemas/`, `src/scoring/`, `src/validator/`, `src/gen/` — **parked.** `Case`/`World`/`Stage` schemas, case-scoring logic, the world-consistency validator, and the series generators — all built for the case-study product's `World` model. Working, tested code, not deleted — may resume in a later phase. The console's own schema (see the handoff doc §4/§5) is a different shape and lives elsewhere once it exists.
